export interface GamesInterface {
    _count: {
        gameId: A[];
    };
    gameId: string;
}
interface A {
    gameId: number;
}
export interface TwGame {
    box_art_url: string;
    id: string;
    name: string;
}
